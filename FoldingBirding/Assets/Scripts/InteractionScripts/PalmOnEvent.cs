using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PalmOnEvent : MonoBehaviour
{
    // Start is called before the first frame update
    public void OnLog(string msg)
    {
        Debug.Log(msg);
    }
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
